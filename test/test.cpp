#include <iostream>
#include <queue>
#include <cmath>
#include <algorithm>

int main () {
    int x = 0, max_x = 5;
    while (x++ < max_x) {
        std::cout << x << std::endl;
    }
    std::cout << "finnally " << x << std::endl;
    return 0;
}